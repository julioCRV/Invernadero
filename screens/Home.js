import React, { useState } from "react";
import { Text, StyleSheet, View } from "react-native";
import Card from '../Components/InvernaderoLista';

export default function Home() {
    return(
         <Card/>
    )
}

const styles = StyleSheet.create({});